import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'lista-compra-ionic-vue-firebase',
  webDir: 'dist'
};

export default config;
